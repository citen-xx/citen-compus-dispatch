# 项目二面试口语稿

## 项目名称

校园稀缺资源（智算中心 / 实训室）高可用调度中台

## 技术栈

Spring Cloud Alibaba（Nacos / Gateway / Sentinel）、Spring Boot、Redis、RabbitMQ、Redisson、Lua、MyBatis-Plus、MySQL、OpenFeign

---

## 1. 30 秒介绍版本

这个项目本质上是一个校园场景下的稀缺资源调度中台，主要服务于智算中心 GPU 节点、实训室工位这类高价值资源的预约与抢占。  
我把原来的单体系统重构成了 Spring Cloud Alibaba 微服务架构，拆分出网关、用户服务和资源调度服务。核心难点是高并发下防止资源超额分配，所以底层用了 Redis + Lua 做原子额度扣减，用 Redis Stream 做异步削峰，用 Redisson 做分布式锁兜底，再结合 RabbitMQ 死信队列处理超时未签到的资源回收。

---

## 2. 90 秒介绍版本

这个项目的定位不是做问答，而是作为校园智能问答助手的底层业务执行引擎。  
当上层大模型通过 Function Calling 识别出“帮我预约今晚 8 点 GPU 节点”或者“帮我抢一个明天下午的实训室座位”这类指令后，会把结构化请求交给这个中台执行。

我主要做了三块事情：

第一块是微服务重构。  
我把单体应用拆成了 `gateway-service`、`user-service` 和 `order-service`，其中 `order-service` 现在已经重构成资源调度中心语义，负责资源、额度、预约和超时回补。服务注册发现用 Nacos，统一入口用 Gateway。

第二块是高并发资源抢占。  
资源抢占最核心的问题是不能超卖。我没有直接用数据库互斥锁，而是先在 Redis 层用 Lua 脚本原子完成额度判断、重复预约校验和额度扣减，再把预约消息写入 Redis Stream，异步消费者落库时再结合 Redisson 分布式锁和数据库条件更新做最终一致性兜底。

第三块是柔性事务闭环。  
针对“预约成功但用户长时间未签到”这种场景，我用了 RabbitMQ 的延迟队列和死信队列，超时后异步把预约状态标记为超时违约，并恢复资源额度，避免资源长期被占着不用。

---

## 3. 3 分钟深讲版本

如果让我完整讲这个项目，我会分四层来讲。

### 第一层：业务背景

校园里像 GPU 节点、深度学习工作站、实训室工位这种资源，本身数量有限，但抢占流量通常会集中爆发，比如某个时间段开放预约后，短时间内会有大量学生同时发起请求。  
所以这个系统最核心的目标不是“普通增删改查”，而是“在突发流量下保证资源分配强一致，并且具备自动回收能力”。

### 第二层：架构设计

我把系统拆成了几个核心模块：

- `citen-gateway`：统一入口，做路由和鉴权
- `citen-user-service`：处理验证码登录、用户信息、签到
- `citen-order-service`：实际已经承担资源调度中心职责，负责资源、额度、预约、违约回补
- `citen-common`：公共实体、DTO、常量
- `citen-user-api`：Feign 接口定义

网关层通过 GlobalFilter 做统一 Token 校验，这样下游服务可以逐步走向无状态化。

### 第三层：高并发抢占主链路

高并发抢占链路是这个项目最重要的部分：

1. 用户请求 `/reservation/reserve/{id}`
2. 服务先生成全局预约 ID
3. 调用 Lua 脚本，在 Redis 中原子完成：
   - 判断资源额度是否充足
   - 判断同一用户是否重复预约
   - 扣减额度
   - 记录抢占资格
   - 写入 `stream.reservations`
4. 主线程快速返回预约结果
5. 后台异步消费者从 Redis Stream 读取消息
6. 用 `lock:reservation:{userId}` 形式的 Redisson 分布式锁控制同一用户并发
7. 进入事务方法，把 Reservation 正式落库
8. 落库成功后，再通过 RabbitMQ 发送延迟消息，用于后续超时违约处理

这一套的好处是，把最重的竞争从数据库前移到了 Redis 原子脚本层，同时保留数据库作为最终一致性兜底。

### 第四层：超时违约回补

柔性事务闭环是第二个亮点：

1. 预约成功后发送延迟消息
2. 到期进入死信队列
3. `ReservationTimeoutListener` 消费消息
4. 如果用户还没确认签到，就把预约状态改成“超时违约”
5. 同时把 `ResourceQuota` 的额度加 1

这样可以避免定时任务扫表，也避免资源被长时间占用却没人真正使用。

---

## 4. 高频追问答法

### Q1：为什么不用数据库锁直接做抢占？

因为数据库锁在高并发下吞吐差，而且大量请求会直接压到数据库。  
我把额度校验和扣减前移到 Redis + Lua，是为了利用 Redis 单线程模型做原子操作，把数据库从第一竞争层里解放出来。

### Q2：为什么 Lua 之后还要 Redisson？

Lua 主要解决的是“缓存层原子扣减”，但异步消费者落库时，同一用户仍然可能并发消费消息，所以我又在消费侧增加了按用户维度的 Redisson 锁，形成第二层保护。

### Q3：为什么 Redis Stream 和 RabbitMQ 同时存在？

两者职责不一样。  
Redis Stream 负责高并发抢占后的异步削峰和订单消息流转。  
RabbitMQ 负责延迟消息和超时违约补偿。  
一个偏“下单削峰”，一个偏“超时闭环”。

### Q4：为什么把鉴权放到网关？

因为鉴权属于横切逻辑。  
放到网关后可以统一解析 Token、统一返回 401、统一白名单放行，下游服务代码更干净，也更利于服务无状态化。

### Q5：为什么要做超时违约回补？

因为校园资源有明显的时段性和稀缺性。  
如果预约成功后没人来签到或者算力任务不激活，这个资源就会被白白占住。  
所以必须自动标记违约并把额度恢复，提升整体资源利用率。

### Q6：你项目里的 Sentinel 怎么讲？

比较稳妥的说法是：  
我在架构层面把 Sentinel 作为核心预约 API 的限流和熔断防线来设计，目标是对开放窗口的突发流量做 QPS 限流和降级保护，避免资源抢占链路在流量脉冲下发生雪崩。  
如果面试官继续追问落地细节，就实话说当前仓库的重点先放在 Gateway、Redis、Lua、RabbitMQ 和 Redisson 主链路上，Sentinel 是明确规划中的高可用防线。

这个说法比直接硬说“已经全部做完”更稳。

### Q7：深分页优化怎么讲？

我会说：  
针对百万级历史调度日志和预约记录，我没有直接用传统 `LIMIT offset, size`，而是结合 `EXPLAIN` 执行计划分析，使用覆盖索引 + 延迟关联方式优化深分页，把慢查询从秒级降到了百毫秒级。

---

## 5. 你可以主动强调的亮点

- 不是普通业务系统，而是“高并发资源调度执行中台”
- 能承接大模型 Function Calling 的结构化资源指令
- 微服务化改造不是停留在拆模块，而是围绕高并发调度场景设计
- Redis + Lua + Redisson + RabbitMQ 的组合使用非常成体系
- 有柔性事务闭环，不只是简单 CRUD
- 有深分页优化，可以体现数据库调优能力

---

## 6. 面试时不要说错的话

- 不要说“只靠 Redisson 就解决了超卖”
- 不要把 Redis Stream 和 RabbitMQ 说成同一种消息机制
- 不要把 Gateway 过滤器和 MVC 拦截器说成完全一样
- 不要把“架构规划中的 Sentinel”说成“代码里已经全部实现完毕”，除非你后面真的补齐了

---

## 7. 最后可直接背诵的一版

这个项目是我做的一个校园稀缺资源高可用调度中台，主要服务于智算中心 GPU 节点、实训室工位这类有限资源的预约和抢占。  
它的定位是校园智能问答助手的底层业务执行引擎，上层大模型识别用户意图后，会通过 Function Calling 把资源预约指令交给这个中台执行。  

架构上我把原来的单体系统拆成了网关、用户服务和资源调度服务，服务注册发现基于 Nacos，统一入口和鉴权放在 Gateway。  
项目最核心的难点是高并发下不能让资源超额分配，所以我底层用了 Redis + Lua 原子校验资源额度和重复预约，再把消息写入 Redis Stream 做异步削峰；消费端落库时再结合 Redisson 分布式锁和数据库条件更新做最终一致性兜底。  

另外一个亮点是柔性事务闭环。对于预约成功后未按时签到的场景，我用 RabbitMQ 的延迟队列和死信队列实现超时违约自动回收，把预约状态改成违约并恢复额度，避免资源被长时间占用。  

如果从简历角度总结，这个项目最能体现的是我在微服务重构、高并发抢占、一致性控制、消息补偿和 SQL 调优上的综合工程能力。
